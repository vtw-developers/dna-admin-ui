extend type Query {
  applications(server: ServerInput): [Application]
  findAllApplications: [Application]
}

type Application {
    id: ID
    name: String
    restPort: Int
    monitorPort: Int
    server: Server
}
